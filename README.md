# CMake Configurator TUI

Конфигуратор проекта для CMake с TUI интерфейсом.

Настраивыемые параметры:
 - Имя проекта
 - Система сборки

## Конфигурация

```bash
cmake --preset release
cmake --build build
```
